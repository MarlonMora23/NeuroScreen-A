services:
  api:
    command: gunicorn run:app --bind 0.0.0.0:5000 --workers 4 --timeout 120
    environment:
      - FLASK_APP=run.py
      - FLASK_ENV=production
    # sin volumen de código, sin puerto expuesto

  redis:
    command: redis-server --requirepass ${REDIS_PASSWORD} --appendonly yes